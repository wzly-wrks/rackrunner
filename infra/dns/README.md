# DNS Notes

- `app.paf.tools` → GitHub Pages deployment for RackRunner web app.
- `sbx.paf.tools` → Sandbox GitHub Pages deployment.
- `api.paf.tools` → Azure Functions production slot.
- `api.sbx.paf.tools` → Azure Functions sandbox slot.
